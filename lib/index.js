import hub from './hub'
// import bs from 'brisky-stamp'

export default (val, stamp) => hub.create(val)

// add uids to stamps else it sucks -- dont compromise for tests think of that as an after thought
